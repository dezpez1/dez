.DS_Store
__pycache__/
*.pyc
.venv/

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Session data — recordings, transcripts, and summaries never get committed.
# This repo is public and these are voice memos from a trip.
sessions/
recordings/
*.m4a
*.caf
.env

# Source art for the app icon. The committed 1024 icon is the deliverable;
# the full-res original stays local.
*.avif

# Reference photography. The mycelial form was built by looking at a licensed
# stock photo of a real network, and that photo is somebody else's work — this
# repo is public, so it stays local. Shaders in, references out.
shutterstock_*.jpg
shutterstock_*.jpeg
shutterstock_*.png
